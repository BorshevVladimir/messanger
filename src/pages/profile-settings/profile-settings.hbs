{{#PageOverlay}}
	{{#Card title='Настройка профиля'}}
	{{#Form classes='user-settings-form' ref='form' id='profile-settings-form' onSubmit=onSubmit onFocusout=onFocusout }}
		<div class="user-settings-form__avatar">
			{{{ Avatar big=true src="https://allods.mail.ru/forums/image.php?u=309803&dateline=1478711553" }}}
		</div>
		{{#each formInputs}}
			{{{FormInput
				ref=this.ref
				label=this.label
				placeholder=this.placeholder
				classes='user-settings-form__item'
				name=@key
				type=this.type
				validator=this.validator
				error=this.error
				showError=this.showError
			}}}
		{{/each}}

		<div class="user-settings-form__item form-buttons">
			{{#Button}}Сохранить{{/Button}}
			{{#Link
				href='/' onClick=goToPage
			}}
				Назад
			{{/Link}}
			</div>

		{{/Form}}
	{{/Card}}
{{/PageOverlay}}